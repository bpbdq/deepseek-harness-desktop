# 1.1.10

本次为「项目改动」面板补上提交历史。

## 新增

- **项目面板显示最近的提交记录。** 「项目改动」面板下方新增「最近提交」一节，列出最近 20 条提交的短哈希、日期与标题；鼠标悬停在某条上可看到完整哈希、作者与日期。

  项目面板此前的职责是"当前有什么改动"，现在同时回答"最近发生过什么"——两者是看待同一个项目的两个角度，放在一起才完整。

## 说明

- 只显示最近 20 条，不提供分页或无限滚动。这里的目标是快速回顾，而不是替代 git 客户端；需要更早的历史时请在终端里使用 `git log`。
- 提交历史只在项目级面板出现。会话内的「本轮修改审查」标签讲的是"本轮"的改动，与提交历史无关。
- 提交历史与改动面板走同一条安全边界：只有本应用登记过的工作区可以被查询。

## 校验

- 宿主测试扩充到 **30 项**，新增历史相关断言：返回分支名、条数上限生效、每条含短哈希/日期/作者/标题，以及未登记的工作区取历史同样被拒（400）。
- 一次实测输出：`d26dfc6 2026-09-16 init`（在一次性临时仓库上运行）。
