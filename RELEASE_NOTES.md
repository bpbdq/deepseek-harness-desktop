# 1.1.9

本次改进切换项目的体验，并新增改动的撤销能力。

## 修复

- **切换项目不再重启应用。** 此前从「文件 → 打开文件夹」选完目录后，应用会自己重启——窗口消失、白屏十几秒、会话列表重新加载，看起来像崩了。现在改为就地切换：停掉旧的服务端、用新工作区启动、同一个窗口重新加载。**窗口与已打开的会话历史都不会丢**（切换后的服务端使用同一个 harness home）。

## 新增

- **可以逐个文件还原改动。** 「项目改动」面板与「本轮修改审查」标签里，每个文件都带一个「还原」按钮：首次点击进入确认态，再点一次才真正还原——这是写操作，不该一击生效，误点会丢掉你自己的改动。

  还原的语义按面板而异：项目级还原到 **HEAD**，会话内还原到**本轮开始时的快照**。两者都**只改工作区文件**，不动索引与 HEAD，因此还原前的状态仍可由 git 找回。

## 注意

- 1.1.7 曾把改动概览挂到 `conversation.composer.bar`，而该槽位是 `single` 类型，导致官方注册失败、界面无法加载。1.1.8 已修回。本版延续该修复。

## 校验

- 宿主测试 **22 项**全部通过，含还原相关的安全断言：拒绝 `/etc/passwd`、`../outside.txt`、`a/../../b.txt`、空字符串等路径；拒绝空路径列表；还原后文件内容确实恢复；**索引未被暂存**（证明只动工作区）；还原后的文件从改动列表消失而其它改动仍在。
