# 1.2.8

本次为文档更新：说明 Windows 首次运行会被 SmartScreen 拦截，以及彻底解决它的唯一办法。

## 说明

- **Windows 安装包没有代码签名**，因此首次运行时 Windows 会弹出「Windows 已保护你的电脑」，提示发布者未知。`.exe` 与 `.msi` 都一样——这是 Windows 对「从网上下载、无可信签名、声誉不足」的程序的既定行为，**不是安装包损坏或被篡改**。

  处理方式是点「更多信息」→「仍要运行」。这几个字很小、容易被忽略，README 里已写明。

- **只有购买代码签名证书能彻底去掉这个提示。** EV 证书即时生效；OV 证书仍需积累声誉，但不再显示"发布者未知"。仅靠时间推移不会让它消失——与 macOS 的 Gatekeeper 不同，SmartScreen 对**每个新版本**都会重新评估（按文件哈希与签名证书），因此每个新 Release 都会再提示一次。

- 中英文 README 均已补齐这一节，含处理步骤与两条本质说明。

## 校验

- `scripts/check-readme.mjs` 通过（章节完整、文件内链接与锚点均有效）。
