# 1.1.7

本次新增项目级常驻面板：进入项目即可查看改动，不必先发起对话。

## 新增

- **常驻的「项目改动」面板。** 输入框工具栏（以及项目页的工作区选择器旁）新增入口，显示当前改动的文件数；点击后从右侧展开一块常驻面板，列出每个变更文件的状态、增删行数，并可逐文件展开差异。

  面板的在项目页与会话内都可用，展开状态会被记住（下次打开仍保持）。

- 项目页的面板列出**未提交的改动**（相对 HEAD）。这与会话内「本轮修改审查」是两种语义：后者回答"这一轮改了什么"，前者回答"这个项目现在有什么改动"。

## 说明

- **这块面板是自绘的，不是官方右侧栏。** 官方右侧栏的内容槽带会话作用域，在项目页（没有会话）时根本不渲染——实测此时调用其打开接口会静默返回而不报错。因此要满足"项目级常驻"只能自己管理面板的展开与位置。
- 面板配色使用官方主题变量，因此在浅色系统下不会出现不合时宜的深色块。

## 校验

- 新增 `scripts/test-project-panel.mjs`：断言项目页入口存在、默认收起、点击后展开、展开后完整落在视口内、以及内容区有实际文本。
- 宿主侧新增工作区级差异路由，原 14 项断言（含"不污染用户索引与暂存列表"）保持通过。
