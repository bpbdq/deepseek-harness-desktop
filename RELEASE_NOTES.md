# 1.2.6

本次修复抽屉的关闭方式，以及还原新建文件时的报错。

## 修复

- **点击抽屉外部不再无反应。** 抽屉此前只能靠右上角的 × 关闭——分支菜单早就有「点外部关闭」，但抽屉一直没有。现在点击外部或按 `Esc` 都会关闭；点击入口按钮本身不算外部（否则会先关再开、出现一闪）。

- **还原「本轮新建的文件」不再报错。** 未跟踪的新文件在基线里没有对应内容，而还原用的是 `git restore --source <基线>`——对基线里不存在的路径会直接失败。现在按路径逐个判断：基线里有就恢复，基线里没有就**删除**它（那才是"回到基线状态"）。

## 校验

- 宿主测试扩到 **35 项**，新增：
  - 未跟踪的新文件**必须出现在改动列表里**（状态 `A`）且差异里含其内容；
  - 该文件**可以被还元**，还原后从列表消失。

- 新增 `scripts/test-drawer-dismiss.mjs`，断言四种关闭/保持行为：点内部不关、点外部关闭、按 `Esc` 关闭、点入口是"打开"而不是一闪一关。全部通过。

- 新增 `scripts/probe-untracked-in-diff.mjs`：用一次性临时仓库验证未跟踪文件确实进入差异。这一条排除了"host 漏掉未跟踪文件"这一候选根因。

## 说明

- 测试里踩到一个自身缺陷并已修正：`data-review-trigger` 标在**外层 div**（供抽屉判定"哪块区域不算外部"），而 `onClick` 在**内层 button** 上。点 div 不会触发 `onClick`，测试因此一度误报"点击无效"——实际是点错了元素。
