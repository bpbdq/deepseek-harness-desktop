# 1.2.1

本次修复切换项目后界面卡在「自动重连中」的问题。

## 修复

- **切换项目后不再卡在「自动重连中」。** 1.2.0 起切换项目不再重启应用（只换服务端），但渲染进程里仍保留着**上一个项目**的持久化状态：dsh 把「当前选中的会话」和「工作区视图」存在浏览器的 `dsh.sessions.current` 与 `dsh.workspace.view.v*` 里。切换后这些指向旧项目里的会话，新服务端不认识，客户端于是一直重试一个不存在的会话——服务端其实早已就绪。

  现在切换前会先清掉这些状态，再导航到新服务端。丢掉的只是「选中的是哪一个会话」，会话数据本身在服务端，不受影响。

## 说明

- 输入框上方那一排（「选择工作区 / 标准模式」所在行）**不提供扩展点**，第三方无法在不破坏输入框的前提下插入控件。实测两种做法都失败：直接注册该槽位会让官方注册失败、界面显示「Failed to load plugins」；用显式优先级遮蔽则会把**输入框本身**顶掉。因此分支徽章与改动概览继续留在下方工具栏。
