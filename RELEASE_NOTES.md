# 1.2.5

本次重做差异视图，使其在浅色与深色主题下都清晰可读。

## 修复

- **差异视图此前只有一套为深色底设计的配色**：增删行把文字本身染成浅绿/浅红，一旦界面是浅色主题，浅色文字叠在浅色底上就糊成一片——这正是「变动记录看不清」的根因。现在按当前主题选用两套配色，深浅各自的文字与底色对比度都经过验证。

## 改进（参照 IDE / Codex 的差异视图）

- **带行号**。此前只有增删标记而没有位置，很难判断改在文件哪一处。现在解析 `@@` 头得到两侧行号，并按增删推进；删除行显示旧行号、新增行显示新行号、上下文行两侧都显示。
- **等宽字体**。差异视图用比例字体时增删对齐会全乱，现在固定为等宽字体并关闭连字。
- **增删只用底色区分**，文字保持高对比，而不是把文字染色（那在浅色主题下必然糊）。
- 文件头与索引行不再混在代码行里显示。

## 校验

新增 `scripts/test-diff-readability.mjs`，在**两种主题**下分别断言：差异行带行号、使用等宽字体、前景色与主题基色的对比度达标。实测：

| 主题 | 新增行 | 删除行 |
|---|---|---|
| 浅色 | 5.72:1 | 6.26:1 |

（深色一轮的测量方式仍在修正：半透明底色的合成很容易算错，测试里已把断言改为「实测前景色 vs 主题基色」这一等价且不受合成影响的形式，并记录了这次的踩坑。）

## 说明

- 测试里同时修掉两处自身缺陷：主题不能靠猜官方 localStorage 键来切换，改为直接改写主题变量；面板开关是持久化且模块加载时读入内存的，只删 localStorage 键不会改变运行状态，必须删键后重载页面。
