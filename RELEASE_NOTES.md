# 1.1.1

本次为维护版本，修复一处界面缺陷。

## 修复

- **修正首次启动解包进度的百分比显示。** 1.1.0 中该进度可能显示为超过 100% 的数值（例如 444%）。原因是百分比由两个量纲不一致的数据相除得出：分子为解包后的原始字节数（约 197 MB），分母为压缩归档的大小（约 43 MB）。

  现改为依据压缩归档的读取进度计算，并在显示前将取值限定在 0–100 区间。进度回调的参数语义已一并统一，相关测试补充了取值范围断言，以避免同类问题再次发生。

## 影响范围

- 仅影响首次启动时的进度提示，**不影响解包结果与运行时功能**。已安装 1.1.0 的用户会在下次全新安装该版本时看到正确的进度。
- 1.1.0 引入的体积优化（安装包 124 MB、装后占用 323 MB）保持不变。

## 校验

- 解包流程的自动化测试在原有 10 项断言基础上，新增 4 项针对进度取值的断言（回调被调用、最大值不超过 100、最小值不小于 0、最终值接近 100），全部通过。
