# 1.2.4

本次调整项目改动面板的形态与位置，并修复分支列表的可读性。

## 修复

- **右上角的入口不再挡住窗口控件与页面图标。** 它此前贴在窗口顶边（距顶部 10px），正好压在最小化/最大化/关闭按钮那一带，在有对话时也会挡住头部的功能图标。现已下移到顶边下方约 44px。

- **分支列表的当前分支不再看不清。** 它此前是浅蓝文字叠在中蓝底上，而按钮又处于 `disabled` 状态——禁用会让整项**半透明**，两者叠加导致当前分支几乎读不出来。现在改为不透明实底 + 高对比文字，并显式把禁用态的透明度重置。远程分支的 `R` 标记也从 55% 不透明度提到 80%。

- 分支列表的底色改用官方主题变量，因此浅色系统下不再是一块深色。

## 变更

- **项目改动面板改为右侧全高抽屉**（与 IDE 的提交面板一致），不再是从入口下方弹出的小浮层。受益之处：内容区更高，提交记录能一屏看到更多；且因为贴住窗口右边、占满高度，不会与窗口控件或页面头部图标抢位置。

## 校验

- 扩展 `scripts/test-menu-anchor.mjs`：

  | 断言 | 实测 |
  |---|---|
  | 入口已下移 | top 44–70（原先 10–36） |
  | 抽屉贴住窗口右边 | right 1424 = 视口宽 1424 |
  | 抽屉占满纵向 | top 0、bottom 855 = 视口高 |
  | 抽屉未占满全屏 | left 863（约 39% 宽度） |
  | 分支项对比度 | 当前分支 4.23:1，其余 15.97:1 |
  | 分支项未被整体半透明 | opacity 全为 1 |

  对比度用 WCAG 相对亮度算出，因此"看不清"这件事从主观判断变成了可断言的数值。
