# 1.1.4

本次修复「本轮改动审查」在大型仓库上无法工作的问题，并改进了它的性能。

## 修复

- **面板一直显示「本轮暂无改动」，即使本轮确实改了文件。** 根因是基线快照的 git 命令超时：快照会对整个工作区执行一次暂存，在带有大量未跟踪文件的仓库上远超原定的 30 秒上限而被中断，于是基线从未记录成功，面板只能报告"没有改动"。

  现已放宽超时，并让客户端在没有基线且当前空闲时**自动补记**。此前基线只在会话由空闲转为运行的那一次记录，有两个场景会永久漏掉：应用启动或切换会话时该轮已经在运行；以及记录请求失败后再无重试。

## 性能

- **取差异从每次约 4 秒降到约 0.5 秒。** 原因是每次查询都重建 git 索引，因而对所有变动文件重新计算哈希。现在复用一份常驻索引，git 依据其 stat 缓存跳过未变化的文件。实测（一个含 6640 个变动路径的仓库）：

  | 操作 | 耗时 |
  |---|---|
  | 记录基线（每轮一次） | 约 8.6 秒 |
  | 取差异（面板打开期间每 10 秒一次） | **约 0.46 秒** |

  记录基线时会顺带预热该索引，因此第一次打开面板也不会再等一次全量扫描。

- 界面上的计数改为每 10 秒刷新一次（原为 4 秒）。该计数晚几秒更新无感，而轮询本身要让宿主核对工作区状态，因此没有必要更频繁。

## 校验

- 宿主侧 14 项断言全部通过，包含「修改 / 新增 / 删除」三类变更均被列出、未改动文件不在列表、以及**整个过程不污染用户的索引与暂存列表**。
- 新增 `scripts/measure-review-live.mjs`：对运行中的实例连续调用路由并计时，用来确认延迟落在可用范围（而非只看单次结果）。
