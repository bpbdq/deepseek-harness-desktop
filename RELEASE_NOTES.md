# 1.2.7

本次把「还原」的确认改成弹窗，并补上一条端到端的还原流程验证。

## 变更

- **还原确认改为弹窗。** 此前是"再点一次这个按钮"的二次确认，有两个毛病：按钮本身很小，第二次点击容易落空（感觉像点了没反应）；而且按钮失焦时会清掉确认态，操作显得不可靠。现在点击「还原」会弹出确认框，写明要还原哪个文件、以及它的内容会回到基线状态（本轮新建的文件会被删除），再由你明确决定。`Esc` 或点击遮罩即取消。

## 关于「AI 新增的文件还原不了」

排查结论：**这不是缺陷，而是安装的版本过旧。**

删除逻辑（未跟踪的新文件在基线里没有对应内容，还原应当删除它）是 **1.2.6 才加入**的：1.2.1 至 1.2.5 都只有 `git restore`，对基线里不存在的路径必然失败。实测确认：

| 标签 | 是否含删除逻辑 |
|---|---|
| v1.2.1 – v1.2.5 | 否 |
| v1.2.6 | 是 |

如果安装的仍是 1.2.1，还原未跟踪文件就会失败。

## 校验

- 新增 `scripts/test-revert-flow.mjs`：在真实实例上走完整流程——造一个未跟踪的新文件 → 在抽屉里找到它 → 点还原 → 弹窗确认 → 断言文件真的被删除、并从列表消失。**六项断言全部通过**：

  ```
  PASS  抽屉里列出了这个新文件
  PASS  点击了还原按钮
  PASS  弹出了确认框
  PASS  点击了确认
  PASS  文件已被删除
  PASS  文件已从列表消失（5 个文件 → 4 个文件）
  ```

- 另加两个一次性探针（`probe-revert-untracked.mjs`、`probe-index-consistency.mjs`），分别排除了两个候选根因：服务端确实能删除未跟踪文件；未跟踪文件被写进临时索引**不会**让"删除"在两次快照间不可见。两个假设都被实验推翻，因此最终定位到版本问题。
