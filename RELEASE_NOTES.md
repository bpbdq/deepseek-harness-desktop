# 1.2.3

本次修复下拉面板飘到屏幕角落的问题，并包含一个来自社区贡献的更新窗口修复。

## 修复

- **更新窗口此前会卡在「正在检查」。** 页面脚本里有一处变量重复声明，导致语法错误、整段脚本不执行，于是版本号、检查结果与按钮全都不会更新。同时把最新状态缓存起来，在页面加载完成、订阅就绪后补发一次，避免检查结果早于订阅而丢失。（感谢 [@bpbdq](https://github.com/bpbdq) 的贡献。）

- **分支菜单不再飘到左下角。** 它此前用固定偏移定位（`bottom: clamp(72px, 12vh, 140px)`、`left: clamp(12px, 3vw, 40px)`），于是钉在屏幕左下角、与分支徽章完全脱节。

  这个偏移是上一版为了修另一个问题引入的：面板原先相对输入框里的小容器定位，会被容器裁成一条（小窗口下只能看到顶部）。改用 `fixed` 确实跳出了裁剪，但 `fixed` 不再跟随入口——偏移写死就飘走了。

  现在打开时**测量徽章的实际位置**，把菜单贴在它正上方并对齐其左边缘；窗口尺寸变化或滚动时会重新测量。项目改动面板同理，贴在它的入口按钮下方。

## 校验

- 新增 `scripts/test-menu-anchor.mjs`：断言的不是"面板在视口内"（那太弱，飘到角落也满足），而是**面板与入口的位置关系**——水平方向必须有重叠、且面板位于入口的正确一侧。实测：

  | 面板 | 入口纵向范围 | 面板纵向范围 | 结论 |
  |---|---|---|---|
  | 分支菜单 | 478–506 | 162–472 | 在徽章正上方，水平重合 |
  | 项目改动 | 10–36 | 42–604 | 在按钮正下方，右对齐 |

- 顺带修掉两处工具问题：`cdp-eval.mjs` 端口写死 9222（被上一个实例占住时会连到旧实例、读到过期状态），改用 `DSH_CDP_PORT` 覆盖；测试断言不要回传整段文本，序列化体积过大会被截成 `undefined` 而产生假失败。
