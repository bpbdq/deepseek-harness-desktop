# 1.1.8

本次新增改动的撤销（还原）能力，并修复一处会导致界面无法加载的缺陷。

## 新增

- **可以逐个文件还原改动。** 「项目改动」面板与「本轮修改审查」标签里的每个文件都带一个「还原」按钮：首次点击进入确认态，再点一次才真正还原（这是写操作，不该一击生效——误点会丢掉你自己的改动）。

  还原的语义按面板而异：项目级还原到 **HEAD**，会话内还原到**本轮开始时的快照**。两者都**只改工作区文件**，不动索引与 HEAD，因此还原前的状态仍可由 git 找回。

## 修复

- **1.1.7 在会话内可能让界面完全无法加载。** 上一版把概览入口挂到了 `conversation.composer.bar`，而该槽位是 **single** 类型（只允许一个注册）——占用它会让官方自己的注册失败，界面显示「Failed to load plugins」，比原本想解决的布局拥挤严重得多。现已改回 `conversation.input.right`（list 类型，可容纳多个注册）。

- 概览入口的文案从「N 个文件」改为纯数字，减少这一排的宽度占用；完整含义放在悬停提示里。

## 说明

- 输入框**上方**那一排（「选择工作区 / 标准模式」所在行）在官方实现里是 single 槽位，第三方无法在不破坏官方注册的前提下往里加控件。因此这个入口只能留在下方工具栏；要放到上方需要官方提供对应的 list 槽位。

## 校验

- 宿主测试扩充到 **22 项**，新增还原相关断言：拒绝 `/etc/passwd`、`../outside.txt`、`a/../../b.txt`、空字符串等不安全路径；拒绝空路径列表；还原后文件内容确实恢复；**索引未被暂存**（证明只动工作区）；还原后的文件从改动列表消失而其它改动仍在。
- `scripts/list-slot-kinds.mjs` 新增：列出各槽位类型（single / list / keyed），避免再次因为挂错类型而顶掉官方注册。
