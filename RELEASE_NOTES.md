# 1.3.1

本次修复「项目改动」面板识别错项目的问题。

## 修复

- **面板现在自动识别当前项目，不再显示别的仓库。** 此前它可能显示应用自己的仓库而不是你在用的项目。根因是**取值的优先级链断了**：面板挂在全局覆盖层上，该位置不按会话作用域注入 `useSessions`，因此"最近会话的工作区"这一环拿不到值，于是退到候选列表的第一项——那恰好是另一个项目。

  现改为由**宿主直接回答**"当前是哪个工作区"：宿主进程启动时就已经 `chdir` 到了工作区，`process.cwd()` 是唯一不需要推断的来源。客户端只把它作为首选，仍保留手动选择（候选多于一个时显示选择器）作为兜底。

- 宿主返回的当前工作区会**先经安全边界校验**：只有在它本身也属于已登记工作区时才回传，避免客户端拿一个必然被拒的路径去请求。

## 校验

- 宿主测试扩到 **39 项**，新增四条针对"当前工作区"的断言：返回候选列表、候选含本仓库、返回了 `current`、且 `current` 就是本仓库。全部通过。

- 新增 `scripts/probe-project-workspace-choice.mjs`：打印面板取值链上每一环的实际值（决定工作区的那一行、推断顺序、是否调用 `/roots`、宿主的来源），用于自查这类"选错了"的问题——它正是本次定位根因的依据。
