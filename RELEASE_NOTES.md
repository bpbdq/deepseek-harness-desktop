# 1.0.9（待发布）

## 修复

- **分支徽章现在跟随「会话的工作区」**。此前它固定查外壳启动时的工作区，因此你在应用里为会话换一个项目后，徽章仍显示上一个仓库的分支。现在会话换项目，徽章立即跟着变（15 秒内也会自动对齐一次外部改动）。
- **只有本应用登记过的工作区才会被查询**。这是安全边界：否则任何能访问本机回环地址的页面都能让宿主进程对任意目录执行 git 命令。
- 修正一处参数校验漏洞：查询参数缺失时会抛错并返回 500，现在返回 400 并说明原因。

## 说明

- 本版本不含界面改动，功能与 1.0.8 相同，只修复上述问题。
