# 1.1.5

本次修复界面主题跟随系统的问题，并把「本轮改动审查」改为使用自带的右侧栏呈现。

## 修复

- **界面不再固定为深色。** 此前外壳把应用外观写死为深色，而这会连带让网页界面也固定为深色——把系统设为浅色的用户看到的仍是深色界面。现改为跟随系统，原生外观（菜单栏、原生对话框）与对话界面因此保持一致。

- **含二进制内容的改动不再显示成乱码。** git 对二进制文件只输出一行说明而非逐行差异，此前那行会被当成普通文本显示。现在会明确标注该文件是二进制内容。

- **超长行不再撑破布局。** 被压缩成单行的文件（例如内嵌数据的 JSON）此前会让差异区域横向溢出、看起来像乱码；现在改为自动折行。

## 变更

- **「本轮改动审查」改用自带的右侧栏呈现。** 输入框工具栏上的入口显示本轮改动的文件数，点击后在右侧栏打开「本轮修改审查」标签：上方是一行汇总（文件数、增删行数），下方是文件列表，每个文件可展开查看差异。

  相比此前的自制浮层，用侧边栏的好处是标签的关闭、拖拽、全屏都由官方侧边栏统一管理，与文档预览等既有标签行为一致，也不会在小窗口下被裁剪。

## 校验

- 新增 `scripts/test-review-sidebar.mjs`：断言「概览入口存在 → 点击 → 侧边栏出现审查标签 → 标签正文有内容」这条链路。全部通过。
- 定位过程中发现的两个坑已写入代码注释：侧边栏标签需要**分别**注册「标签类型」与「渲染槽位」，只注册槽位会让点击表现为毫无反应；以及侧边栏在会话尚未采纳时会静默返回而不报错。
