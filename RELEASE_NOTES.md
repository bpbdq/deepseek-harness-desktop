# 1.1.3

本次为维护版本，修复小窗口下两个下拉面板被裁剪的问题。

## 修复

- **窗口较小时，输入框工具栏上的两个面板会被遮住。** 受影响的是「本轮改动审查」面板与分支切换菜单：它们此前以相对工具栏容器的定位方式渲染，而该容器位于输入框卡片内部，面板因此被卡片的可视区域裁掉——小窗口里只能看到顶部一条。

  现改为相对视口定位，并为宽高加上视口约束，因此在任何窗口尺寸下都完整可见。

## 校验

- 新增 `scripts/test-panel-geometry.mjs`：通过浏览器调试协议把视口调整为常规窗口（1440×900）与小屏（800×600）两种尺寸，打开两个面板并测量其边界矩形，断言四边均不越出视口、且定位方式为视口定位。**14 项断言全部通过**，小屏下的实测边界为：

  | 面板 | 顶 | 左 | 右 | 底 |
  |---|---|---|---|---|
  | 本轮改动审查 | 48 | 14 | 776 | 137 |
  | 分支切换菜单 | 218 | 24 | 327 | 528 |

  这类缺陷此前只能靠肉眼看截图发现——把几何变成可断言的数值后，回归时不会再漏。
