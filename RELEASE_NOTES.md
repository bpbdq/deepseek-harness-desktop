# 1.2.2

本次让项目级「改动与提交记录」面板真正可用。

## 修复

- **项目级入口此前从未渲染出来**，因此看不到任何项目级信息。它原先挂在「选择工作区」那一行的槽位上，而该槽位是 `single` 类型、官方自己也在注册——我的注册被静默顶掉，从未生效。现在改为挂在全局覆盖层上，入口是右上角一个固定按钮，在项目页与会话内都可用。

- **工作区列表此前恒为空**，面板一直显示「选择要查看的项目」。原因是该接口被放在了工作区校验之后，而它回答的正是「有哪些工作区可以看」——要求请求先带一个合法工作区，是个先有鸡还是先有蛋的问题。现已调整到校验之前。

- **并发请求会撞出 git 索引锁**（界面上直接显示 `Unable to create '...index.lock': File exists`）。面板加载与计数轮询会同时打同一个路由，两个 git 进程同时写同一份索引。现已把并发的相同快照合并为一次计算。

## 新增/完善

- **项目面板现在同时显示两件事**：当前未提交的改动（逐文件状态、增删行数、还原按钮），以及**最近的 20 条提交记录**（短哈希、日期、标题）。

- 失败不再只是停在「加载中」：取不到数据时会显示具体原因，例如未选择项目时会明确提示。

## 校验

- 新增 `scripts/test-project-panel.mjs`：断言入口存在且位于视口内、点击后面板展开、面板四边不越出视口、含「最近提交」一节、**历史里确实有提交条目**、以及能确定工作区。全部通过。
- 一次实测输出（面板开头与结尾）：

  ```
  项目改动 | 5 个文件 | 5 个文件，+262 −103 | 修改 | plugins/dsh-client-ui-review/lib/client.js | +103 −19 | 还原
  … ca20fb6 | 2026-09-16 | fix(review): 修复基线无法记录，并把取差异从 4s 降到 0.5s
  ```
