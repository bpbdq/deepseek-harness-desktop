# 1.1.2

本次新增「本轮修改审查」功能。

## 新增

- **每轮任务结束后可查看本轮改动。** 输入框工具栏新增「本轮改动」入口，显示本轮 agent 修改的文件数；点击展开面板，列出每个变更文件的状态与增删行数，并可逐文件查看统一差异。

  基线的取法：一轮对话**开始时**为当前工作区拍一张 git 快照，本轮结束后的改动即相对该快照计算。因此即使你在本轮开始前就有未提交改动，那些改动也**不会**被算进本轮。

- 基线快照使用独立的临时索引文件完成（`GIT_INDEX_FILE`），**不改动**你的索引、暂存列表与 HEAD。相关工作区状态不受影响这一点已由自动化测试断言。

## 说明

- 快照覆盖未跟踪文件（即本轮新建的文件也会被列出），并遵循 `.gitignore` 规则。
- 基线按会话保存在宿主进程内存中，**进程重启后需重新开始一轮**才会再次记录基线。
- 切换会话的工作区后，旧基线自动失效，面板会提示需重新开始一轮。
- 仅覆盖 git 仓库；非 git 工作区会给出提示。
