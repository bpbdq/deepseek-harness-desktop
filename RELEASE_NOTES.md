# 1.1.0（待发布）

## 变更

- **安装包与磁盘占用大幅缩小。** 安装包从 152 MB 降到 124 MB，装完后占用从 582 MB 降到 323 MB。做法是把内置运行时先瘦身、再用 brotli 压成单个归档随包携带，首次启动解压到用户目录。

## 注意

- **首次启动会多等约 9 秒**，用于解压内置运行时（约 197 MB / 10 000 个文件）。启动页会显示解压进度。之后每次启动不受影响。
- 解压位置是应用数据目录下的 `bundled-runtime/`，与"运行时自动更新"存放位置分开，互不干扰。
- 安装包体积的剩余部分主要是 Electron 外壳本身（压缩后约 110 MB，其中 `electron.exe` 单个 180 MB，内嵌 Chromium 与 V8，无法再压）。**要再显著变小必须换掉 Electron**，那会失去"装完即用、不联网也能跑"的前提。
